const pct = value => `${Math.round((Number(value) || 0) * 100)}%`;
const ms = value => Number.isFinite(value) ? `${Math.round(value)} ms` : 'not measured';
const issue = ({id,category,severity,title,detail,impact,effort='s',evidence,recommendation,observed,expected,location}) => ({id,category,severity,title,detail,impact,effort,evidence,recommendation,observed,expected,location});

export function deriveDetailedIssues(scan){
  const out=[];
  const d=scan.dom||{},m=scan.metrics||{},c=scan.checks||{},h=scan.headers||{};
  const location=scan.url||'Primary page';
  const add=x=>out.push(issue({...x,location:x.location||location}));
  const mobileLcp=m.mobileLcpMs??m.lcpMs;

  if(!c.httpOk) add({id:'site-unreachable',category:'robustness',severity:'critical',title:'Primary page is not reachable',detail:`The benchmark could not obtain a successful HTTP response from the primary URL${c.status?` (HTTP ${c.status})`:''}.`,impact:100,effort:'xs',evidence:`HTTP status: ${c.status??'no response'}.`,observed:c.status??'No response',expected:'HTTP 200–399 and a renderable document',recommendation:'Check the deployment, DNS, redirects and runtime logs. Restore a successful response before any design or optimization work.'});
  if(!scan.url?.startsWith('https://')) add({id:'https',category:'securityTrust',severity:'critical',title:'HTTPS is not enforced',detail:'The final page URL is not using HTTPS, which weakens transport security and user trust.',impact:100,effort:'s',evidence:`Final URL: ${scan.url||'unknown'}.`,observed:scan.url||'unknown',expected:'HTTPS final URL',recommendation:'Enable TLS and redirect all HTTP traffic permanently to the HTTPS canonical URL.'});
  if(!d.viewportMeta) add({id:'viewport-meta',category:'mobile',severity:'high',title:'Mobile viewport meta tag is missing',detail:'Mobile browsers may render the page at a desktop layout width instead of the device width.',impact:90,effort:'xs',evidence:'No <meta name="viewport"> was detected in the document head.',observed:'Missing',expected:'width=device-width, initial-scale=1',recommendation:'Add a viewport meta tag in the document head and verify the 390 px mobile capture again.'});
  if(c.horizontalOverflow) add({id:'mobile-overflow',category:'mobile',severity:'high',title:'Page overflows horizontally on mobile',detail:'At the 390 px benchmark viewport, the document is wider than the visible viewport and can scroll sideways.',impact:92,effort:'s',evidence:'scrollWidth exceeded innerWidth by more than 3 px during the mobile scan.',observed:'Horizontal overflow detected at 390 px',expected:'No horizontal scrolling at 390 px',recommendation:'Inspect fixed-width containers, transformed elements, full-bleed media and long unbroken content. Constrain the offending element to the viewport.'});
  if(Number.isFinite(mobileLcp)&&mobileLcp>4000) add({id:'slow-lcp',category:'performance',severity:mobileLcp>6000?'high':'medium',title:'Largest Contentful Paint is too slow',detail:`The main visible content takes ${ms(mobileLcp)} to become the largest painted element on the mobile run.`,impact:84,effort:'m',evidence:`Measured mobile LCP: ${ms(mobileLcp)}.`,observed:ms(mobileLcp),expected:'≤ 2,500 ms',recommendation:'Identify the LCP element, then prioritize its image/font request, remove render-blocking work, compress oversized media and reduce main-thread work before it paints.'});
  if(Number.isFinite(m.cls)&&m.cls>0.1) add({id:'layout-shift',category:'performance',severity:m.cls>0.35?'high':'medium',title:'Layout shifts after initial render',detail:`The measured CLS is ${Number(m.cls).toFixed(3)}, meaning visible content moves while the page is loading.`,impact:78,effort:'m',evidence:`Measured CLS: ${Number(m.cls).toFixed(3)}.`,observed:Number(m.cls).toFixed(3),expected:'≤ 0.100',recommendation:'Reserve dimensions for images/embeds, avoid injecting content above existing UI, and ensure web fonts do not cause large geometry changes.'});
  if(Number.isFinite(m.ttfbMs)&&m.ttfbMs>800) add({id:'slow-ttfb',category:'performance',severity:m.ttfbMs>2000?'high':'medium',title:'Server response is slow',detail:`The initial document response took ${ms(m.ttfbMs)} before the browser received the first byte.`,impact:72,effort:'m',evidence:`Measured TTFB: ${ms(m.ttfbMs)}.`,observed:ms(m.ttfbMs),expected:'≤ 800 ms',recommendation:'Review server rendering, origin latency, uncached data requests and CDN/cache configuration. Make the HTML response cacheable where appropriate.'});
  if((d.tinyTapTargetRatio||0)>0.05) add({id:'tiny-targets',category:'mobile',severity:(d.tinyTapTargetRatio||0)>0.35?'high':'medium',title:'Interactive targets are too small on mobile',detail:`${pct(d.tinyTapTargetRatio)} of detected links/buttons are smaller than the 44×44 px touch target used by the benchmark.`,impact:76,effort:'s',evidence:`Affected ratio: ${pct(d.tinyTapTargetRatio)}${d.tinyTapTargetSamples?.length?`. Examples: ${d.tinyTapTargetSamples.join(' · ')}`:''}.`,observed:pct(d.tinyTapTargetRatio),expected:'≤ 5% below 44×44 px',recommendation:'Increase the hit area of the listed controls using padding/min-size without necessarily making the visual icon itself larger.'});
  if((d.tinyTextRatio||0)>0.02) add({id:'tiny-text',category:'visualDesign',severity:(d.tinyTextRatio||0)>0.22?'high':'medium',title:'Text is too small to read comfortably',detail:`${pct(d.tinyTextRatio)} of visible sampled elements render below 12 px.`,impact:62,effort:'s',evidence:`Affected ratio: ${pct(d.tinyTextRatio)}${d.tinyTextSamples?.length?`. Examples: ${d.tinyTextSamples.join(' · ')}`:''}.`,observed:pct(d.tinyTextRatio),expected:'≤ 2% of visible text below 12 px',recommendation:'Raise the smallest typography tokens and verify metadata/helper text on mobile and desktop.'});
  if(!d.titleLength) add({id:'page-title',category:'seoGeo',severity:'high',title:'HTML title is missing',detail:'The primary document has no meaningful <title>, so search engines and browser tabs lack the page’s primary identity.',impact:76,effort:'xs',evidence:'Detected title length: 0 characters.',observed:'Missing',expected:'A unique, descriptive title',recommendation:'Add a concise title that includes the business/entity and the page intent without keyword stuffing.'});
  else if(d.titleLength<25||d.titleLength>65) add({id:'page-title-length',category:'seoGeo',severity:'low',title:'HTML title length is outside the preferred range',detail:`The title is ${d.titleLength} characters long, which may be too vague or truncate in search results.`,impact:38,effort:'xs',evidence:`Detected title length: ${d.titleLength} characters.`,observed:`${d.titleLength} characters`,expected:'About 25–65 characters',recommendation:'Rewrite the title so it communicates the page and business clearly within a practical SERP length.'});
  if(!d.metaDescriptionLength) add({id:'meta-description',category:'seoGeo',severity:'medium',title:'Meta description is missing',detail:'No meta description was found for the primary page, leaving search engines to construct an uncontrolled snippet.',impact:62,effort:'xs',evidence:'Detected meta description length: 0 characters.',observed:'Missing',expected:'A unique descriptive meta description',recommendation:'Add a specific description summarizing the offer and value proposition for this page.'});
  else if(d.metaDescriptionLength<70||d.metaDescriptionLength>180) add({id:'meta-description-length',category:'seoGeo',severity:'low',title:'Meta description length is weak',detail:`The description is ${d.metaDescriptionLength} characters long and may be too thin or unnecessarily long.`,impact:34,effort:'xs',evidence:`Detected meta description length: ${d.metaDescriptionLength} characters.`,observed:`${d.metaDescriptionLength} characters`,expected:'About 70–180 characters',recommendation:'Rewrite it to concisely describe the offer, location/context and primary differentiator.'});
  if(!d.canonical) add({id:'canonical',category:'seoGeo',severity:'medium',title:'Canonical URL is missing',detail:'The primary page does not declare its preferred canonical URL.',impact:52,effort:'xs',evidence:'No link[rel="canonical"] was detected.',observed:'Missing',expected:'Self-referencing canonical on the primary URL',recommendation:'Add a canonical tag pointing to the preferred absolute URL for this page.'});
  if(d.h1Count!==1) add({id:'h1-structure',category:'content',severity:'medium',title:d.h1Count===0?'Primary H1 is missing':'Multiple H1 headings detected',detail:`The page contains ${d.h1Count||0} H1 elements. The benchmark expects one clear primary heading for the page.`,impact:55,effort:'xs',evidence:`H1 count: ${d.h1Count||0}${d.headingSamples?.length?`. Headings: ${d.headingSamples.join(' · ')}`:''}.`,observed:`${d.h1Count||0} H1 elements`,expected:'Exactly 1 H1',recommendation:'Keep one page-level H1 and demote secondary section headings to H2/H3 according to hierarchy.'});
  if((d.maxHeadingJump||0)>1) add({id:'heading-hierarchy',category:'accessibility',severity:'medium',title:'Heading hierarchy skips levels',detail:`The document jumps by up to ${d.maxHeadingJump} heading levels, which weakens structure for assistive technology and scanning.`,impact:48,effort:'s',evidence:`Maximum heading-level jump: ${d.maxHeadingJump}.`,observed:`Jump of ${d.maxHeadingJump}`,expected:'No jump larger than 1 level',recommendation:'Reorder H2/H3/H4 levels to represent the actual page hierarchy rather than visual size.'});
  if(!d.lang) add({id:'html-lang',category:'accessibility',severity:'medium',title:'Document language is not declared',detail:'The <html> element has no lang attribute, making pronunciation and language handling less reliable for assistive technology and search engines.',impact:44,effort:'xs',evidence:'document.documentElement.lang was empty.',observed:'Missing',expected:'Correct BCP 47 language code',recommendation:'Set the html lang attribute to the primary page language, and update it correctly on localized versions.'});
  if(!d.jsonLdCount) add({id:'structured-data',category:'seoGeo',severity:'medium',title:'Structured data is missing',detail:'No JSON-LD structured data was detected on the primary page.',impact:64,effort:'s',evidence:'JSON-LD script count: 0.',observed:'0 JSON-LD blocks',expected:'Relevant valid schema for the business/page type',recommendation:'Add only schema that matches visible content, such as Organization/LocalBusiness/Product/RealEstateAgent/Restaurant where applicable.'});
  if(!c.robots) add({id:'robots',category:'seoGeo',severity:'medium',title:'robots.txt is unavailable',detail:'The site did not return a successful /robots.txt response.',impact:42,effort:'xs',evidence:'GET /robots.txt did not return a successful response.',observed:'Unavailable',expected:'Accessible robots.txt',recommendation:'Publish a valid robots.txt and reference the sitemap when appropriate.'});
  if(!c.sitemap) add({id:'sitemap',category:'seoGeo',severity:'medium',title:'sitemap.xml is unavailable',detail:'The benchmark could not retrieve /sitemap.xml successfully.',impact:48,effort:'s',evidence:'GET /sitemap.xml did not return a successful response.',observed:'Unavailable',expected:'Accessible XML sitemap',recommendation:'Generate and publish an XML sitemap containing canonical indexable URLs, then reference it in robots.txt.'});
  if((d.imagesMissingAltRatio||0)>0.02) add({id:'image-alt',category:'accessibility',severity:(d.imagesMissingAltRatio||0)>0.45?'high':'medium',title:'Images are missing alternative text',detail:`${pct(d.imagesMissingAltRatio)} of detected images do not have a non-empty alt attribute.`,impact:62,effort:'s',evidence:`Affected ratio: ${pct(d.imagesMissingAltRatio)}${d.imagesMissingAltSamples?.length?`. Examples: ${d.imagesMissingAltSamples.join(' · ')}`:''}.`,observed:pct(d.imagesMissingAltRatio),expected:'Meaningful images described; decorative images intentionally alt=""',recommendation:'Add concise alt text to informative images and explicit empty alt text to decorative images.'});
  if((d.unlabelledFormControlRatio||0)>0.01) add({id:'form-labels',category:'accessibility',severity:'high',title:'Form controls are missing accessible labels',detail:`${pct(d.unlabelledFormControlRatio)} of visible form controls have no associated label, aria-label or equivalent accessible name.`,impact:82,effort:'s',evidence:`Affected ratio: ${pct(d.unlabelledFormControlRatio)}${d.unlabelledFormControlSamples?.length?`. Examples: ${d.unlabelledFormControlSamples.join(' · ')}`:''}.`,observed:pct(d.unlabelledFormControlRatio),expected:'Every interactive field has an accessible label',recommendation:'Associate visible <label> elements with each control or provide an appropriate accessible name. Do not rely on placeholder text alone for important fields.'});
  if((d.unnamedButtonRatio||0)>0.01) add({id:'unnamed-controls',category:'accessibility',severity:'high',title:'Interactive controls have no accessible name',detail:`${pct(d.unnamedButtonRatio)} of visible links/buttons do not expose readable text, aria-label or title.`,impact:82,effort:'xs',evidence:`Affected ratio: ${pct(d.unnamedButtonRatio)}${d.unnamedButtonSamples?.length?`. Examples: ${d.unnamedButtonSamples.join(' · ')}`:''}.`,observed:pct(d.unnamedButtonRatio),expected:'Every interactive control has an accessible name',recommendation:'Add visible text or a precise aria-label to icon-only and otherwise unnamed controls.'});
  if(c.formsHaveAction===false) add({id:'form-submit',category:'robustness',severity:'high',title:'A form has no detectable submission path',detail:'At least one form has neither an action URL nor a submit control, so its completion path may be broken or unclear.',impact:86,effort:'m',evidence:'formsHaveAction returned false during the browser scan.',observed:'Submission path missing on ≥1 form',expected:'Every user-facing form has a working submission path',recommendation:'Verify the form handler, submit control, client-side event and success/error states end to end.'});
  if((d.brokenInternalLinkRatio||0)>0) add({id:'broken-links',category:'robustness',severity:(d.brokenInternalLinkRatio||0)>0.2?'high':'medium',title:'Internal links return errors',detail:`${d.brokenInternalLinks?.length||0} sampled internal link(s) failed the HTTP check.`,impact:78,effort:'s',evidence:d.brokenInternalLinks?.length?`Broken URLs: ${d.brokenInternalLinks.join(' · ')}`:`Broken-link ratio: ${pct(d.brokenInternalLinkRatio)}.`,observed:`${d.brokenInternalLinks?.length||0} broken sampled link(s)`,expected:'0 broken internal links',recommendation:'Repair, redirect or remove each listed URL and verify navigation from the originating page.'});
  if((scan.console?.errorCount??0)>0) add({id:'console-errors',category:'robustness',severity:'high',title:'Browser runtime errors were detected',detail:`The desktop/mobile scans produced ${scan.console.errorCount} JavaScript runtime error(s).`,impact:78,effort:'m',evidence:scan.console.errors?.length?scan.console.errors.join(' · '):`${scan.console.errorCount} runtime error(s).`,observed:`${scan.console.errorCount} error(s)`,expected:'0 uncaught runtime errors',recommendation:'Open the affected route with production source maps/logging, reproduce each listed exception and fix the originating code path rather than suppressing the error.'});
  if((scan.network?.failedRequestCount??0)>0) add({id:'failed-requests',category:'robustness',severity:'high',title:'Network requests fail during page load',detail:`The scans recorded ${scan.network.failedRequestCount} failed network request(s), which can leave content, fonts, images or functionality incomplete.`,impact:80,effort:'m',evidence:scan.network.failures?.length?scan.network.failures.join(' · '):`${scan.network.failedRequestCount} failed request(s).`,observed:`${scan.network.failedRequestCount} failed request(s)`,expected:'0 unexplained failed production requests',recommendation:'Fix each listed request URL/error, including incorrect paths, blocked CORS requests, missing assets and aborted critical API calls.'});
  if(!d.primaryCtaCount) add({id:'primary-cta',category:'conversion',severity:'high',title:'No clear primary CTA was detected',detail:'The page does not expose a visible action matching common booking, reservation, enquiry, contact or purchase intent.',impact:86,effort:'m',evidence:'Primary CTA detector count: 0.',observed:'0 detected primary CTAs',expected:'At least one obvious primary commercial action',recommendation:'Define the page’s single primary conversion action and make it explicit in the hero/navigation with business-specific wording.'});
  if(!d.contactSignalCount&&!d.formCount&&!d.bookingSignalCount) add({id:'contact-path',category:'conversion',severity:'high',title:'No detectable enquiry/contact path',detail:'The page has no detected tel/mail link, contact form or booking/reservation signal.',impact:88,effort:'m',evidence:`tel/mail links: ${d.contactSignalCount||0}; forms: ${d.formCount||0}; booking signals: ${d.bookingSignalCount||0}.`,observed:'No detectable commercial contact path',expected:'A clear, usable conversion path',recommendation:'Provide a direct, context-preserving route to enquire, reserve, book or contact the business and verify it works on mobile.'});
  if(!d.hasPrivacyLink) add({id:'privacy-link',category:'securityTrust',severity:'low',title:'Privacy policy link was not detected',detail:'The page does not expose a detectable privacy-policy link.',impact:34,effort:'xs',evidence:'No anchor matching privacy/privacidad was detected.',observed:'Missing',expected:'Accessible privacy policy',recommendation:'Link the current privacy policy from an appropriate persistent location such as the footer and relevant forms.'});
  if(!d.hasLegalLink) add({id:'legal-link',category:'securityTrust',severity:'low',title:'Legal/terms/cookies link was not detected',detail:'No legal, terms or cookies link was found in the page links.',impact:30,effort:'xs',evidence:'No anchor matching legal/terms/cookies was detected.',observed:'Missing',expected:'Applicable legal information accessible to users',recommendation:'Expose the legal notice/terms/cookie information required for the site and jurisdiction.'});
  if(scan.url?.startsWith('https://')&&!h.hsts) add({id:'hsts',category:'securityTrust',severity:'low',title:'HSTS header is missing',detail:'The HTTPS response does not advertise Strict-Transport-Security.',impact:34,effort:'xs',evidence:'strict-transport-security response header not detected.',observed:'Missing',expected:'HSTS enabled after HTTPS is stable',recommendation:'Add a suitable Strict-Transport-Security header at the hosting/CDN layer after confirming all required subdomains support HTTPS.'});
  if(!h.csp) add({id:'csp',category:'securityTrust',severity:'medium',title:'Content Security Policy is missing',detail:'No Content-Security-Policy response header was detected, reducing browser-side protection against unintended script/content execution.',impact:50,effort:'m',evidence:'content-security-policy response header not detected.',observed:'Missing',expected:'A tested CSP appropriate to the application',recommendation:'Introduce a report-only CSP first, inventory required origins, then enforce a least-permissive policy without breaking legitimate assets.'});

  return out;
}
